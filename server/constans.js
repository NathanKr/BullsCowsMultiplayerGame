// --- event name issued by server
const messages = {
  // --- event name issued by server
  newMessage: "newMessage",

  // --- event names issued by client
  createMessage: "createMessage",

  joinRoom: "joinRoom",

  newUser: "newUser"
};

const admin = "Admin";

module.exports = {
  messages,
  admin
};
